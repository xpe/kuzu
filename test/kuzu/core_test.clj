(ns kuzu.core-test
  (:require [clojure.test :refer :all]
            [kuzu.core :as k]))

(deftest client-test
  (testing "client-test"
    :TODO))

(deftest clients-test
  (testing "clients-test"
    :TODO))

(deftest connected-clients-test
  (testing "connected-clients-test"
    :TODO))

(deftest available-clients-test
  (testing "available-clients-test"
    :TODO))

(deftest long-poll-test
  (testing "long-poll"
    :TODO))

(deftest available-client-test
  (testing "available-client"
    :TODO))

(deftest eval-test
  (testing "eval"
    :TODO))

(deftest map-test
  (testing "map"
    :TODO))

(deftest filter-test
  (testing "filter"
    :TODO))

(deftest reduce-test
  (testing "reduce"
    :TODO))
